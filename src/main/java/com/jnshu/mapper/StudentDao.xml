<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jnshu.mapper.StudentDao">
    <select id="findByIdStudent" resultType="Student" parameterType="int">
        SELECT * FROM tb_students WHERE id = #{id}
    </select>
    <select id="findListStudent" resultType="Student">

    </select>
    <insert id="insertStudent" parameterType="Student">

    </insert>
    <delete id="deleteStrudent" parameterType="int">

    </delete>

    <update id="updateStudent" parameterType="Student">

    </update>

    <select id="countStudent" parameterType="Student" resultType="Integer">

    </select>
</mapper>